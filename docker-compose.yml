services:
  runner:
    image: github-runner:latest
    build:
      context: .
      args:
        RUNNER_VERSION: 'arm-2.321.0'
    environment:
      GH_TOKEN: ${GH_TOKEN}
      GH_OWNER: ${GH_OWNER}
      GH_REPOSITORY: ${GH_REPOSITORY}
    #env_file:
    #  - ./vars.env
